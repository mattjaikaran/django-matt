---
phase: 03-cli-and-type-generation
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - django_matt/cli/commands/status.py
  - django_matt/cli/commands/analyze.py
  - django_matt/cli/main.py
  - django_matt/management/commands/matt_migrate_from.py
  - django_matt/management/commands/matt_status.py
  - django_matt/management/commands/matt_analyze.py
  - tests/test_cli_module.py
  - tests/test_management_commands.py
autonomous: true
requirements: [DX-07, DX-08, DX-09, DX-10, DX-11]

must_haves:
  truths:
    - "matt doctor reports errors (broken: missing settings), warnings (suboptimal: debug=True), and info (suggestions)"
    - "matt routes shows compact table (Method | Path | Handler); --verbose adds request/response schema details"
    - "matt migrate-from ninja rewrites django-ninja imports to django-matt and adds TODO markers for ambiguous patterns"
    - "All CLI commands work non-interactively with flags (CI-friendly)"
    - "AsyncAPITestClient.force_authenticate() uses acreate_access_token() (verified from Phase 1)"
    - "Test factories and assertion helpers work correctly (verified existing 67 tests)"
    - "Example apps (todo, ecommerce, saas-starter, realtime-chat) pass ruff lint"
  artifacts:
    - path: "django_matt/cli/commands/status.py"
      provides: "Doctor command with Error/Warning/Info tiers"
      contains: "error.*warning.*info"
    - path: "django_matt/cli/commands/analyze.py"
      provides: "Routes command with --verbose flag"
      contains: "verbose"
    - path: "django_matt/management/commands/matt_migrate_from.py"
      provides: "Django-ninja migration tool with TODO markers"
      contains: "TODO"
    - path: "tests/test_cli_module.py"
      provides: "CLI tests for doctor, routes, migrate-from"
      contains: "test_doctor"
  key_links:
    - from: "django_matt/cli/main.py"
      to: "django_matt/cli/commands/status.py"
      via: "doctor() delegates to status_doctor()"
      pattern: "from django_matt.cli.commands.status import doctor"
    - from: "django_matt/cli/main.py"
      to: "django_matt/cli/commands/analyze.py"
      via: "routes() delegates to analyze_routes()"
      pattern: "from django_matt.cli.commands.analyze import routes"
    - from: "django_matt/cli/main.py"
      to: "django_matt/management/commands/matt_migrate_from.py"
      via: "migrate_from() calls run_manage_command"
      pattern: "migrate_from"
---

<objective>
Complete Rich CLI commands (doctor, routes), django-ninja migration tool, and verify testing/example requirements.

Purpose: CLI is the primary developer interface. Doctor and routes are the most-used introspection commands. The migration tool helps django-ninja users adopt django-matt. DX-09/10/11 are verification of existing work.
Output: Working doctor with tiered output, routes with --verbose, migration tool with TODO markers, verified examples.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-type-generation/03-CONTEXT.md
@.planning/phases/03-cli-and-type-generation/03-RESEARCH.md

@django_matt/cli/main.py
@django_matt/cli/commands/status.py
@django_matt/cli/commands/analyze.py
@django_matt/management/commands/matt_migrate_from.py
@django_matt/management/commands/matt_status.py
@django_matt/management/commands/matt_analyze.py
@django_matt/testing/client.py
@django_matt/testing/factories.py
@django_matt/testing/assertions.py
@tests/test_cli_module.py
@tests/test_management_commands.py
@tests/test_testing_module.py

<interfaces>
<!-- Key interfaces from CLI module -->

From django_matt/cli/main.py:
```python
# Top-level aliases that delegate to subcommands
def doctor(verbose: bool = False): ...   # delegates to status_doctor()
def routes(verbose: bool = False): ...   # delegates to analyze_routes()
def migrate_from(source: str, path: str = "."): ...  # calls run_manage_command
```

From django_matt/cli/commands/status.py (296 lines):
- doctor() command exists but needs Error/Warning/Info tier structure
- Currently uses Rich console for output

From django_matt/cli/commands/analyze.py (268 lines):
- routes() command exists but needs --verbose flag for schema details
- Currently shows basic route listing

From django_matt/management/commands/matt_migrate_from.py (1000 lines):
- Substantial migration tool exists
- Needs verification that import rewrites work correctly
- Needs verification that TODO markers are placed for ambiguous patterns

From django_matt/testing/client.py:
- AsyncAPITestClient.force_authenticate() uses acreate_access_token() — DONE in Phase 1
- DX-09 is satisfied — just needs test verification

From django_matt/testing/factories.py:
- ModelFactory, UserFactory exist and work — 67 tests pass
- DX-10 is satisfied — just needs test verification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete doctor and routes CLI commands</name>
  <files>django_matt/cli/commands/status.py, django_matt/cli/commands/analyze.py, django_matt/management/commands/matt_status.py, django_matt/management/commands/matt_analyze.py, tests/test_cli_module.py</files>
  <action>
    1. In `django_matt/cli/commands/status.py` (and/or `matt_status.py` management command), implement the tiered doctor output:
       a. Define a `CheckResult` dataclass or dict with fields: `tier` (error/warning/info), `name`, `message`, `fix` (optional)
       b. Error tier checks (must fix):
          - SECRET_KEY not configured or is default "change-me"
          - django_matt not in INSTALLED_APPS
          - Required settings missing (DATABASES not configured)
          - Import failures for required modules
       c. Warning tier checks (should fix):
          - DEBUG=True in apparent production (check DJANGO_SETTINGS_MODULE for "prod" or "production")
          - No cache backend configured (using default LocMemCache)
          - ALLOWED_HOSTS is empty
          - No CORS headers configured
       d. Info tier checks (suggestions):
          - Recommend MATT_API_MODE for API-only projects
          - Suggest caching configuration
          - Note available but unused features (e.g., DI container not configured)
       e. Use Rich tables/panels: Red for errors, Yellow for warnings, Blue for info
       f. Summary line: "X errors, Y warnings, Z info" with appropriate color

    2. In `django_matt/cli/commands/analyze.py` (and/or `matt_analyze.py`), add `--verbose` flag to routes:
       a. Default output: compact table with columns Method | Path | Handler
       b. `--verbose` output: add columns for Request Schema | Response Schema | Permissions
       c. Extract schema info from route's endpoint type hints (`get_type_hints()` on the handler)
       d. Extract permissions from controller's `permission_classes`

    3. Add tests to `tests/test_cli_module.py`:
       - `test_doctor_error_tier`: mock missing SECRET_KEY, assert "error" tier reported
       - `test_doctor_warning_tier`: mock DEBUG=True, assert "warning" tier reported
       - `test_doctor_info_tier`: assert info suggestions appear
       - `test_doctor_summary_line`: assert output ends with "X errors, Y warnings, Z info"
       - `test_routes_compact_output`: assert table has Method, Path, Handler columns
       - `test_routes_verbose_output`: assert --verbose adds schema columns
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_cli_module.py -k "doctor or routes" -x -q</automated>
  </verify>
  <done>matt doctor reports errors/warnings/info with Rich formatting and summary line. matt routes shows compact table by default, --verbose adds schema details. All CLI tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify migration tool, testing module, and example apps</name>
  <files>django_matt/management/commands/matt_migrate_from.py, tests/test_management_commands.py, tests/test_cli_module.py</files>
  <action>
    1. Verify `matt_migrate_from.py` migration tool functionality:
       a. Review the existing 1000-line implementation
       b. Ensure it rewrites `from ninja import Router` to `from django_matt.core.router import APIRouter`
       c. Ensure it rewrites `from ninja import Schema` to `from pydantic import BaseModel`
       d. Ensure it adds `# TODO: Review this migration — manual verification needed` markers for:
          - Decorator patterns that don't have a 1:1 mapping
          - Complex authentication middleware rewrites
          - Custom exception handlers
       e. If any rewrite logic is missing or broken, fix it

    2. Add/verify tests in `tests/test_management_commands.py`:
       - `test_migrate_from_ninja_rewrites_imports`: provide a sample ninja file, run migration, assert imports are rewritten
       - `test_migrate_from_ninja_adds_todo_markers`: assert TODO markers appear for ambiguous patterns
       - `test_migrate_from_ninja_preserves_business_logic`: assert non-ninja code is unchanged

    3. Verify DX-09 (AsyncAPITestClient) is complete:
       - Run `uv run pytest tests/test_testing_module.py -k "authenticate" -x -q`
       - Confirm force_authenticate() uses acreate_access_token()

    4. Verify DX-10 (Test factories and assertions) is complete:
       - Run `uv run pytest tests/test_testing_module.py -x -q`
       - Confirm all 67+ tests pass

    5. Verify DX-11 (Example apps) pass lint:
       - Run `uv run ruff check examples/` and fix any violations
       - Common fixes: update deprecated imports, fix unused imports, modernize type annotations
       - If example apps reference old APIs changed in Phases 1-2, update the imports

    6. Ensure all CLI commands work non-interactively (CI-friendly):
       - Verify `matt doctor`, `matt routes`, and `matt migrate-from ninja` work with flags only (no prompts)
       - Add test asserting no interactive prompts are triggered when flags are provided
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_management_commands.py -k "migrate" -x -q && uv run pytest tests/test_testing_module.py -x -q && uv run ruff check examples/ --quiet</automated>
  </verify>
  <done>Migration tool rewrites ninja imports and adds TODO markers. AsyncAPITestClient confirmed working (DX-09). Test factories confirmed working (DX-10). Example apps pass ruff lint (DX-11). All commands work non-interactively.</done>
</task>

</tasks>

<verification>
cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_cli_module.py tests/test_management_commands.py tests/test_testing_module.py -x -q && uv run ruff check examples/ --quiet
</verification>

<success_criteria>
1. matt doctor reports errors/warnings/info with tiered Rich output
2. matt routes shows compact table; --verbose adds schema details
3. matt migrate-from ninja rewrites imports and adds TODO markers
4. All CLI commands work non-interactively with flags
5. AsyncAPITestClient.force_authenticate() confirmed working (DX-09)
6. Test factories and assertions confirmed working (DX-10, 67+ tests pass)
7. Example apps pass ruff lint (DX-11)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-type-generation/03-04-SUMMARY.md`
</output>
