---
phase: 03-cli-and-type-generation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/core/router.py
  - tests/test_core_controller.py
  - tests/test_di.py
  - tests/test_negotiation.py
  - tests/test_versioning.py
  - tests/test_openapi.py
  - tests/test_views.py
autonomous: true
requirements: [CORE-01, CORE-02, CORE-04, CORE-05, CORE-06, CORE-11, CORE-13, CORE-14, CORE-15]

must_haves:
  truths:
    - "Static routes (/users/me) are matched before parameterized routes (/users/<id>) by the router — verified by test"
    - "Router registers both async and sync views with automatic URL generation"
    - "Controller _setup_methods() wraps DI + error handling in a single closure"
    - "CRUD ViewSet generates all 5 endpoints (list, create, read, update, delete)"
    - "OpenAPI 3.1 schema includes all registered routes with correct methods"
    - "Swagger UI and ReDoc are served at configurable endpoints"
    - "DI container resolves Scoped services per-request via ContextVar"
    - "Content negotiation returns correct format based on Accept header"
    - "API versioning extracts version from URL path, header, or query param"
  artifacts:
    - path: "django_matt/core/router.py"
      provides: "Router with static-before-parameterized URL ordering"
      contains: "_is_parameterized_path"
    - path: "tests/test_core_controller.py"
      provides: "Test proving CORE-11 static URL ordering"
      contains: "test_static_before_parameterized"
  key_links:
    - from: "django_matt/core/router.py"
      to: "django.urls.path"
      via: "get_urls() returns URLPattern list with static-first ordering"
      pattern: "static_patterns.*param_patterns"
    - from: "tests/test_core_controller.py"
      to: "django_matt/core/router.py"
      via: "Test creates routes with /me and /<id> and asserts ordering"
      pattern: "get_urls.*static.*param"
---

<objective>
Verify all 9 CORE requirements pass their tests, and add the missing CORE-11 test for static-before-parameterized URL ordering.

Purpose: CORE requirements are the framework foundation. Phase 3 needs to verify these are all working correctly. CORE-11 has an implementation but no test.
Output: CORE-11 test added; all 9 CORE requirement tests confirmed passing.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-type-generation/03-CONTEXT.md
@.planning/phases/03-cli-and-type-generation/03-RESEARCH.md

@django_matt/core/router.py
@django_matt/core/controller.py
@django_matt/views/viewset.py
@django_matt/openapi/schema.py
@django_matt/openapi/docs.py
@django_matt/di/container.py
@django_matt/negotiation/negotiator.py
@django_matt/versioning/schemes.py
@tests/test_core_controller.py
@tests/test_openapi.py

<interfaces>
<!-- Key interfaces for CORE-11 test -->

From django_matt/core/router.py:
```python
class APIRouter:
    def __init__(self, prefix: str = "", tags: list[str] = None): ...
    def add_route(self, path, endpoint, methods, ...): ...
    def get(self, path, *, response_model=None, ...): ...  # decorator
    def post(self, path, *, response_model=None, ...): ...  # decorator
    def get_urls(self) -> list:
        """Returns URL patterns with static-first ordering.
        Uses _is_parameterized_path() to separate static from parameterized.
        Returns: static_patterns + param_patterns"""

    @staticmethod
    def _is_parameterized_path(url_pattern) -> bool:
        """Return True if URLPattern contains a path converter (e.g. <str:id>)."""
```

CORE-11 implementation exists in get_urls():
- static_patterns = [] and param_patterns = [] collected separately
- _append() helper checks _is_parameterized_path()
- Returns static_patterns + param_patterns

What's missing: A test that registers /users/me AND /users/<str:id> and asserts /users/me appears first in get_urls() output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CORE-11 static-before-parameterized URL ordering test</name>
  <files>tests/test_core_controller.py, django_matt/core/router.py</files>
  <behavior>
    - Test: When routes /users/me and /users/&lt;str:id&gt; are registered (in any order), get_urls() returns /users/me before /users/&lt;str:id&gt;
    - Test: When routes /items/featured, /items/&lt;int:id&gt;, and /items/&lt;int:id&gt;/reviews are registered, /items/featured sorts first
    - Test: Multiple static routes at same depth preserve their declaration order
    - Test: _is_parameterized_path correctly identifies parameterized vs static URL patterns
  </behavior>
  <action>
    1. Add test class `TestStaticBeforeParameterizedOrdering` to `tests/test_core_controller.py`:

       a. `test_static_route_before_parameterized`:
          - Create an APIRouter
          - Register a handler at path "users/<str:id>" (parameterized)
          - Register a handler at path "users/me" (static)
          - Call `router.get_urls()`
          - Assert the static pattern (users/me) appears before the parameterized pattern (users/<str:id>) in the returned list
          - Extract the route string from each URLPattern to verify ordering

       b. `test_multiple_static_routes_preserve_order`:
          - Register "items/featured", "items/popular", "items/<int:id>"
          - Assert featured and popular appear before <int:id>
          - Assert featured appears before popular (declaration order preserved within static group)

       c. `test_is_parameterized_path_detection`:
          - Create URLPattern objects for static and parameterized paths
          - Assert _is_parameterized_path returns False for static, True for parameterized

    2. If the current implementation in `get_urls()` doesn't handle edge cases correctly (e.g., nested parameterized paths like `/items/<int:id>/reviews/<int:review_id>`), fix the sorting logic.

    3. Verify the implementation handles controller-registered routes too (not just decorator routes) — the `_append` helper in get_urls() is used for both code paths.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_core_controller.py -k "static" -x -v</automated>
  </verify>
  <done>CORE-11 has a dedicated test proving static routes sort before parameterized routes. Edge cases (multiple statics, nested params) are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all CORE requirements pass existing tests</name>
  <files>tests/test_core_controller.py, tests/test_views.py, tests/test_openapi.py, tests/test_di.py, tests/test_negotiation.py, tests/test_versioning.py</files>
  <action>
    1. Run the full CORE verification test suite and confirm all pass:
       - CORE-01 (Router async/sync): `pytest tests/test_core_controller.py -x -q`
       - CORE-02 (Controller DI+error): `pytest tests/test_core_controller.py tests/test_di_autowire.py -x -q`
       - CORE-04 (CRUD ViewSet): `pytest tests/test_views.py -x -q`
       - CORE-05 (OpenAPI schema): `pytest tests/test_openapi.py -x -q`
       - CORE-06 (Swagger/ReDoc): `pytest tests/test_openapi.py -k "docs or swagger or redoc" -x -q`
       - CORE-11 (Static URLs): `pytest tests/test_core_controller.py -k "static" -x -q` (from Task 1)
       - CORE-13 (DI container): `pytest tests/test_di.py -x -q`
       - CORE-14 (Content negotiation): `pytest tests/test_negotiation.py -x -q`
       - CORE-15 (API versioning): `pytest tests/test_versioning.py -x -q`

    2. If any tests fail, investigate and fix the root cause. Common issues:
       - Import errors from Phase 1/2 changes
       - Async test configuration issues
       - Missing test fixtures

    3. Document any gaps found: if a CORE requirement has no specific test, add a minimal test proving the behavior. For example:
       - CORE-01: If no test proves both async and sync registration work, add `test_router_registers_sync_and_async_views`
       - CORE-06: If no test proves configurable endpoints, add `test_swagger_at_custom_path`

    4. Run the combined verification:
       ```
       uv run pytest tests/test_core_controller.py tests/test_views.py tests/test_openapi.py tests/test_di.py tests/test_negotiation.py tests/test_versioning.py -x -q
       ```
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_core_controller.py tests/test_views.py tests/test_openapi.py tests/test_di.py tests/test_negotiation.py tests/test_versioning.py -x -q</automated>
  </verify>
  <done>All 9 CORE requirements (CORE-01, 02, 04, 05, 06, 11, 13, 14, 15) have passing tests. Any gaps identified have been filled with minimal tests.</done>
</task>

</tasks>

<verification>
cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_core_controller.py tests/test_views.py tests/test_openapi.py tests/test_di.py tests/test_negotiation.py tests/test_versioning.py -x -q
</verification>

<success_criteria>
1. CORE-11 has a dedicated test proving /users/me sorts before /users/<id>
2. All 9 CORE requirement test suites pass (controller, views, openapi, di, negotiation, versioning)
3. No test regressions from any changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-type-generation/03-03-SUMMARY.md`
</output>
