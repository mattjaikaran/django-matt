---
phase: 03-cli-and-type-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/management/commands/generate_crud.py
  - django_matt/management/commands/startapi.py
  - django_matt/benchmarks/reporters.py
  - tests/test_management_commands.py
autonomous: true
requirements: [DX-01, DX-02]

must_haves:
  truths:
    - "generate_crud --full produces controller, schema, service, admin, and test files that pass ruff check without modification"
    - "Generated code is async-first with service layer pattern — controller calls service, service calls ORM with async variants"
    - "Generated tests use pytest + django.test.AsyncClient (not @pytest.mark.asyncio since asyncio_mode=auto)"
    - "startapi creates project scaffolding for basic, b2b, and saas templates"
    - "b2b/saas templates include CLAUDE.md, docker-compose, Makefile, CI config"
  artifacts:
    - path: "django_matt/management/commands/generate_crud.py"
      provides: "CRUD code generator with ruff-clean templates"
      contains: "model_dump(exclude_unset=True)"
    - path: "django_matt/management/commands/startapi.py"
      provides: "Project scaffolding with template selection"
      contains: "b2b"
    - path: "tests/test_management_commands.py"
      provides: "Tests proving generated code passes ruff lint"
      contains: "test_generate_crud_full_passes_ruff"
  key_links:
    - from: "tests/test_management_commands.py"
      to: "django_matt/management/commands/generate_crud.py"
      via: "management command call_command test"
      pattern: "call_command.*generate_crud"
---

<objective>
Complete the generate_crud and startapi management commands so they produce production-quality output.

Purpose: DX-02 is the highest-visibility success criterion — generated code must pass ruff lint without modification. DX-01 ensures a developer can scaffold a full project from templates.
Output: Working generate_crud --full that produces lint-clean async-first CRUD code; working startapi with basic/b2b/saas templates.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-type-generation/03-CONTEXT.md
@.planning/phases/03-cli-and-type-generation/03-RESEARCH.md

@django_matt/management/commands/generate_crud.py
@django_matt/management/commands/startapi.py
@django_matt/cli/base.py
@tests/test_management_commands.py

<interfaces>
<!-- Key patterns the executor needs -->

From django_matt/cli/base.py:
- GeneratorCommand: base class with --dry-run, --force, write_file(), show_summary()
- write_file(path, content, preview=True) handles conflict detection and file tracking

From generate_crud.py (current issues to fix):
- Generated test file uses `@pytest.mark.asyncio` — REMOVE (asyncio_mode=auto in pyproject.toml)
- Generated test file uses `django.test.AsyncClient` — correct, keep
- Generated schemas use `from typing import Any` — needs Python 3.12 style check
- Generated controller uses sync Q import when not needed with service layer
- Generated service uses `from django.db import transaction` but never uses it async — needs `@sync_to_async` wrapper or removal

From startapi.py (current state):
- Templates: starter (basic), b2b, saas — all exist in code but b2b/saas need CLAUDE.md, CI config
- _create_project(), _create_api_app(), _configure_project(), _create_example() — all exist
- _get_b2b_settings(), _get_b2c_settings() — exist but may need completion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix generate_crud generated code quality</name>
  <files>django_matt/management/commands/generate_crud.py, django_matt/benchmarks/reporters.py, tests/test_management_commands.py</files>
  <behavior>
    - Test: generate_crud --full for a model produces files that pass `ruff check` with zero errors
    - Test: Generated controller uses async ORM throughout (no sync .get(), .save(), .delete())
    - Test: Generated service file exists and contains async methods (aget, acreate, asave, adelete)
    - Test: Generated test file does NOT contain `@pytest.mark.asyncio` decorator
    - Test: Generated schema uses Python 3.12 syntax (X | None not Optional[X], list[X] not List[X])
    - Test: Generated admin file uses django-unfold ModelAdmin
  </behavior>
  <action>
    1. Fix pre-existing ruff lint error: remove unused `StringIO` import from `django_matt/benchmarks/reporters.py`

    2. In `generate_crud.py`, fix the generated code templates:
       a. Remove `@pytest.mark.asyncio` from generated test methods (project uses `asyncio_mode = "auto"`)
       b. Replace any `from typing import Optional, List, Dict` with Python 3.12 builtins (`list[X]`, `X | None`)
       c. Ensure `_generate_service_content()` uses `async def` for all methods and async ORM calls (`aget`, `acreate`, `asave`, `adelete`)
       d. Ensure `_generate_controller_content()` with `with_service=True` does NOT import `django.http.Http404` or `django.db.models.Q` (those are service concerns)
       e. Ensure `_generate_admin_content()` generates Unfold-compatible admin registration (user decision: admin uses Unfold)
       f. Ensure `_generate_schema_content()` uses `list[X]` not `List[X]`, `X | None` not `Optional[X]`

    3. Add test `test_generate_crud_full_passes_ruff` to `tests/test_management_commands.py`:
       - Call `generate_crud` with a test model using `--full --dry-run` or capture generated content
       - Run `ruff check` on each generated file's content (use `subprocess.run(["ruff", "check", "--stdin-filename", ...], input=content)`)
       - Assert zero lint errors

    4. Add test `test_generate_crud_service_async_pattern` verifying generated service methods are async and use async ORM.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_management_commands.py -k "generate_crud" -x -q && uv run ruff check django_matt/benchmarks/reporters.py</automated>
  </verify>
  <done>generate_crud --full produces controller, schema, service, admin, and test files that all pass ruff check without modification. Generated code is async-first. Pre-existing ruff error in reporters.py is fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Complete startapi project scaffolding templates</name>
  <files>django_matt/management/commands/startapi.py, tests/test_management_commands.py</files>
  <action>
    1. In `startapi.py`, complete the b2b and saas templates:
       a. b2b template: Ensure `_configure_project()` with template="b2b" adds auth (JWT), multitenancy, billing scaffolding to settings
       b. saas template: Similar to b2b but with consumer-focused settings (no multitenancy, yes billing)
       c. Both templates: Generate a CLAUDE.md file in the project root with project-specific instructions (stack, conventions, testing commands)
       d. Both templates: Generate CI config (GitHub Actions workflow at `.github/workflows/ci.yml` with lint + test steps)
       e. Both templates: docker-compose.yml with postgres service (already partially exists via `_create_docker_config`)

    2. Verify basic template produces: settings.py with django-matt configured, a single API app, urls.py

    3. Add test `test_startapi_b2b_template_files` to `tests/test_management_commands.py`:
       - Call `startapi` with `--template b2b` into a temp directory
       - Assert CLAUDE.md exists
       - Assert `.github/workflows/ci.yml` exists
       - Assert settings.py contains multitenancy app reference
       - Assert docker-compose.yml exists

    4. Add test `test_startapi_basic_template` verifying basic template produces settings.py, urls.py, and API app directory.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_management_commands.py -k "startapi" -x -q</automated>
  </verify>
  <done>startapi --template basic produces a working project scaffold. startapi --template b2b produces project with auth, multitenancy, CLAUDE.md, CI config, and docker-compose. Tests verify file presence and content.</done>
</task>

</tasks>

<verification>
cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_management_commands.py -x -q && uv run ruff check django_matt/management/commands/generate_crud.py django_matt/management/commands/startapi.py django_matt/benchmarks/reporters.py
</verification>

<success_criteria>
1. `generate_crud --full` produces controller, schema, service, admin, and test files that pass `ruff check` without modification
2. Generated code uses async ORM throughout — no sync .get(), .save(), .delete()
3. Generated tests use pytest without `@pytest.mark.asyncio` (asyncio_mode=auto)
4. `startapi --template b2b` produces project with CLAUDE.md, CI config, docker-compose
5. All generate_crud and startapi tests pass
6. Pre-existing ruff lint error in reporters.py fixed
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-type-generation/03-01-SUMMARY.md`
</output>
