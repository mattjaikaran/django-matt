---
phase: 03-cli-and-type-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/management/commands/sync_types.py
  - django_matt/management/commands/generate_ai_context.py
  - django_matt/typegen/typescript.py
  - django_matt/typegen/zod.py
  - django_matt/typegen/swift.py
  - django_matt/typegen/api_client.py
  - tests/test_typegen.py
  - tests/test_management_commands.py
  - tests/test_ai_context.py
autonomous: true
requirements: [DX-03, DX-04, DX-05, DX-06]

must_haves:
  truths:
    - "sync_types --target typescript produces valid TypeScript interfaces from Pydantic schemas"
    - "sync_types --target zod produces valid Zod schemas with correct z.* types"
    - "sync_types --target swift produces Codable structs AND typed URLSession/async-await API client"
    - "sync_types --from-openapi generates types from OpenAPI spec for drift verification"
    - "generate_ai_context --depth minimal produces routes only"
    - "generate_ai_context --depth full produces routes, types, model relationships, conventions"
    - "generate_ai_context --include-examples pulls actual code snippets from the user codebase"
  artifacts:
    - path: "django_matt/management/commands/sync_types.py"
      provides: "Type sync command with --from-openapi flag"
      contains: "from-openapi"
    - path: "django_matt/typegen/swift.py"
      provides: "Swift generator producing Codable structs + API client"
      contains: "URLSession"
    - path: "django_matt/typegen/api_client.py"
      provides: "Fetch-based TypeScript API client generator"
      contains: "fetch"
    - path: "django_matt/management/commands/generate_ai_context.py"
      provides: "AI context export with --depth flag"
      contains: "depth"
    - path: "tests/test_typegen.py"
      provides: "Type generator tests"
      contains: "test_typescript"
  key_links:
    - from: "django_matt/management/commands/sync_types.py"
      to: "django_matt/typegen/typescript.py"
      via: "TypeScriptGenerator import and generate() call"
      pattern: "TypeScriptGenerator.*generate"
    - from: "django_matt/management/commands/sync_types.py"
      to: "django_matt/typegen/swift.py"
      via: "SwiftGenerator import and generate() call"
      pattern: "SwiftGenerator.*generate"
    - from: "django_matt/management/commands/sync_types.py"
      to: "django_matt/openapi/schema.py"
      via: "--from-openapi flag triggers OpenAPI schema introspection"
      pattern: "OpenAPISchema"
---

<objective>
Complete type generation (TypeScript/Zod/Swift) and AI context export commands with all user-specified features.

Purpose: Type generation is the bridge between django-matt backend and frontend/mobile clients. AI context export enables LLM-assisted development. Both are key DX features.
Output: Working sync_types with --from-openapi flag and all targets; working generate_ai_context with --depth flag.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-and-type-generation/03-CONTEXT.md
@.planning/phases/03-cli-and-type-generation/03-RESEARCH.md

@django_matt/management/commands/sync_types.py
@django_matt/management/commands/generate_ai_context.py
@django_matt/typegen/typescript.py
@django_matt/typegen/zod.py
@django_matt/typegen/swift.py
@django_matt/typegen/api_client.py
@django_matt/openapi/schema.py
@tests/test_typegen.py
@tests/test_ai_context.py

<interfaces>
<!-- Key contracts from existing code -->

From django_matt/typegen/typescript.py:
```python
class TypeScriptGenerator:
    def __init__(self, camel_case: bool = False): ...
    def generate(self, schemas: list[type[BaseModel]]) -> str: ...
    # Uses model_fields (Pydantic v2 API) for introspection
```

From django_matt/typegen/zod.py:
```python
class ZodGenerator:
    def __init__(self, camel_case: bool = False): ...
    def generate(self, schemas: list[type[BaseModel]]) -> str: ...
```

From django_matt/typegen/swift.py:
```python
class SwiftGenerator:
    def __init__(self, camel_case: bool = True): ...
    def generate(self, schemas: list[type[BaseModel]]) -> str: ...
    # Currently generates Codable structs only — needs API client extension
```

From django_matt/typegen/api_client.py:
```python
class APIClientGenerator:
    def __init__(self, base_url: str = "/api", ...): ...
    def generate(self, schemas: list[type[BaseModel]], routes: list = None) -> str: ...
    # Generates fetch-based TypeScript API client
```

From sync_types.py:
- _collect_schemas(): collects Pydantic schemas from modules/apps
- _collect_models(): collects Django models from apps
- _generate(): dispatches to appropriate generator based on --target
- Currently supports: typescript, zod, swift, client, all
- Missing: --from-openapi flag

From generate_ai_context.py:
- _generate(): calls ContextGenerator from django_matt.ai.context
- _get_formats(): returns list of format types
- Missing: --depth flag (minimal/standard/full)
- Has: --format (default/claude/cursor/copilot/json/all)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete type generators and --from-openapi flag</name>
  <files>django_matt/management/commands/sync_types.py, django_matt/typegen/swift.py, django_matt/typegen/api_client.py, tests/test_typegen.py, tests/test_management_commands.py</files>
  <behavior>
    - Test: TypeScriptGenerator.generate() produces valid TS interfaces with correct type mappings (str->string, int->number, UUID->string, datetime->string, list->Array, Optional->union with null)
    - Test: ZodGenerator.generate() produces valid Zod schemas with z.string(), z.number(), z.boolean(), z.array(), z.optional()
    - Test: SwiftGenerator.generate() produces Codable structs with correct type mappings AND a typed APIClient class with async/await methods using URLSession
    - Test: APIClientGenerator.generate() produces fetch-based TypeScript client with typed request/response wrappers
    - Test: sync_types --from-openapi generates types from OpenAPI spec (calls OpenAPISchema.build() and extracts component schemas)
    - Test: sync_types --target swift includes both model structs and API client code
  </behavior>
  <action>
    1. In `django_matt/typegen/swift.py`, extend SwiftGenerator to optionally produce a typed API client:
       a. Add `generate_api_client(schemas, routes=None, base_url="/api")` method
       b. Generate a Swift `APIClient` class using URLSession with `async throws` methods
       c. Use `decoder.keyDecodingStrategy = .convertFromSnakeCase` in the generated client (per research pitfall 3)
       d. Each schema gets typed CRUD methods: `func list{Model}s() async throws -> [{Model}]`, etc.
       e. When `sync_types --target swift` is called, output both structs AND client

    2. In `django_matt/management/commands/sync_types.py`, add `--from-openapi` flag:
       a. Add argument: `--from-openapi` (store_true, default=False)
       b. When enabled, call `OpenAPISchema.build()` to get the schema dict
       c. Extract component schemas from `schema["components"]["schemas"]`
       d. Convert OpenAPI type definitions to Pydantic-compatible type info for the generators
       e. Also add `--openapi-file` for pre-generated schema files (CI use case)

    3. Verify existing TypeScriptGenerator and ZodGenerator produce correct output by reviewing and fixing any edge cases:
       a. Ensure `model_fields` (Pydantic v2) is used, not `__fields__`
       b. Ensure datetime maps to `string` in TS and `z.string().datetime()` in Zod
       c. Ensure UUID maps to `string` in TS and `z.string().uuid()` in Zod

    4. Add tests to `tests/test_typegen.py`:
       - `test_swift_api_client_generation`: SwiftGenerator produces URLSession-based client code
       - `test_typescript_type_mappings`: verify all Python->TS type mappings
       - `test_zod_type_mappings`: verify all Python->Zod type mappings

    5. Add test to `tests/test_management_commands.py`:
       - `test_sync_types_from_openapi_flag`: verify --from-openapi flag is accepted and produces output
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_typegen.py tests/test_management_commands.py -k "typegen or sync_types or swift or typescript or zod" -x -q</automated>
  </verify>
  <done>All three type generators produce correct output. Swift generator includes typed APIClient with URLSession/async-await. sync_types --from-openapi generates types from OpenAPI schema. All type mapping tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Complete generate_ai_context with --depth flag and --include-examples</name>
  <files>django_matt/management/commands/generate_ai_context.py, tests/test_ai_context.py</files>
  <action>
    1. In `generate_ai_context.py`, add `--depth` argument:
       a. Choices: `minimal`, `standard`, `full` (default: `standard`)
       b. `minimal`: routes only (method, path, handler name)
       c. `standard`: routes + types (Pydantic schemas, Django model definitions)
       d. `full`: routes + types + model relationships + conventions + settings overview
       e. The `--depth` flag controls content volume; `--format` controls output file format (CLAUDE.md, .cursorrules, .copilot-instructions, JSON)

    2. Add `--include-examples` flag:
       a. When enabled, scan the user's codebase for controller, schema, and test files
       b. Extract representative code snippets (first controller method, first schema, first test)
       c. Include them in the generated context files as "example patterns from this project"

    3. Ensure each format produces correctly structured output:
       a. CLAUDE.md: detailed, structured markdown optimized for Claude (role, stack, patterns, routes, types)
       b. .cursorrules: rules-based format following Cursor conventions
       c. .copilot-instructions: GitHub Copilot instruction format
       d. JSON: machine-readable format for MCP servers, agent pipelines

    4. Add/extend tests in `tests/test_ai_context.py`:
       - `test_depth_minimal_routes_only`: --depth minimal produces routes but not types
       - `test_depth_full_includes_relationships`: --depth full includes model relationships
       - `test_include_examples_flag`: --include-examples adds code snippets to output
       - `test_format_all_produces_all_files`: --format all creates CLAUDE.md, .cursorrules, .copilot-instructions, and JSON
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_ai_context.py -x -q</automated>
  </verify>
  <done>generate_ai_context --depth minimal/standard/full controls content depth. --include-examples pulls real code snippets. --format all produces all four output formats (CLAUDE.md, .cursorrules, .copilot-instructions, JSON). Tests verify each combination.</done>
</task>

</tasks>

<verification>
cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_typegen.py tests/test_management_commands.py tests/test_ai_context.py -x -q
</verification>

<success_criteria>
1. sync_types --target typescript produces valid TypeScript interfaces from Pydantic schemas
2. sync_types --target zod produces valid Zod schemas
3. sync_types --target swift produces Codable structs AND typed URLSession API client
4. sync_types --from-openapi generates types from OpenAPI spec
5. generate_ai_context --depth minimal/standard/full controls output depth correctly
6. generate_ai_context --include-examples pulls actual code snippets
7. All type generation and AI context tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-and-type-generation/03-02-SUMMARY.md`
</output>
