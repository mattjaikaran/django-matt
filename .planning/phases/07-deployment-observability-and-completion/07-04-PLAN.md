---
phase: 07-deployment-observability-and-completion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/admin/base.py
  - django_matt/admin/dashboard.py
  - django_matt/admin/widgets.py
  - django_matt/admin/generator.py
  - django_matt/graphql/schema.py
  - django_matt/graphql/dataloaders.py
  - django_matt/graphql/views.py
  - django_matt/htmx/response.py
  - django_matt/htmx/components.py
  - django_matt/components/serving.py
  - tests/test_admin_module.py
  - tests/test_graphql.py
  - tests/test_htmx.py
  - tests/test_components.py
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - GQL-01
  - GQL-02
  - GQL-03
  - HTMX-01
  - HTMX-02
  - COMP-01

must_haves:
  truths:
    - "Django Unfold admin integration provides modern admin UI for django-matt models"
    - "Admin dashboard widgets render metric cards and charts"
    - "Admin inline configuration works from django-matt model definitions"
    - "Strawberry schema auto-generates GraphQL types from Django models"
    - "DataLoader integration prevents N+1 in GraphQL queries"
    - "GraphQL endpoint serves alongside REST from same app"
    - "HTMX response helpers produce correct HX-Trigger, HX-Swap, HX-Redirect headers"
    - "Livewire-style components render and handle events"
    - "Backend-served component system renders components from Python"
  artifacts:
    - path: "django_matt/admin/base.py"
      provides: "Unfold-integrated ModelAdmin base"
      contains: "class.*Admin"
    - path: "django_matt/graphql/schema.py"
      provides: "Strawberry schema generation from Django models"
      contains: "strawberry"
    - path: "django_matt/graphql/dataloaders.py"
      provides: "DataLoader for N+1 prevention"
      contains: "DataLoader"
    - path: "django_matt/htmx/response.py"
      provides: "HTMX response helpers"
      contains: "HX-Trigger|hx_trigger"
    - path: "tests/test_graphql.py"
      provides: "GraphQL schema + DataLoader tests"
      contains: "strawberry|graphql"
  key_links:
    - from: "django_matt/graphql/views.py"
      to: "django_matt/graphql/schema.py"
      via: "view serves schema"
      pattern: "schema"
    - from: "django_matt/graphql/schema.py"
      to: "django_matt/graphql/dataloaders.py"
      via: "schema resolvers use dataloaders"
      pattern: "dataloader|DataLoader"
---

<objective>
Verify and complete admin (Unfold integration, dashboard widgets, inlines), GraphQL (Strawberry schema generation, DataLoaders, endpoint), HTMX (response helpers, reactive components), and backend component system. All modules have substantial existing code -- the work is auditing against requirements and ensuring test coverage.

Purpose: Frontend integration modules (admin, GraphQL, HTMX, components) reach v1 quality.
Output: Verified modules with tests covering all requirement behaviors.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-deployment-observability-and-completion/07-RESEARCH.md
@django_matt/admin/base.py
@django_matt/admin/dashboard.py
@django_matt/admin/widgets.py
@django_matt/admin/generator.py
@django_matt/graphql/schema.py
@django_matt/graphql/dataloaders.py
@django_matt/graphql/views.py
@django_matt/htmx/response.py
@django_matt/htmx/components.py
@django_matt/components/serving.py
@tests/test_admin_module.py
@tests/test_graphql.py
@tests/test_htmx.py
@tests/test_components.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit admin, GraphQL, HTMX, and components modules</name>
  <files>
    django_matt/admin/base.py,
    django_matt/admin/dashboard.py,
    django_matt/admin/widgets.py,
    django_matt/admin/generator.py,
    django_matt/graphql/schema.py,
    django_matt/graphql/dataloaders.py,
    django_matt/graphql/views.py,
    django_matt/htmx/response.py,
    django_matt/htmx/components.py,
    django_matt/components/serving.py
  </files>
  <action>
    Read all module files and audit against requirements:

    1. **ADMIN-01 (Unfold integration):** Verify `admin/base.py` integrates with `django-unfold` via `HAS_UNFOLD` guard. Verify ModelAdmin base class inherits from Unfold's admin when available, falls back to Django admin otherwise. Fix any import issues.

    2. **ADMIN-02 (Dashboard widgets):** Verify `admin/dashboard.py` and `admin/widgets.py` provide widget classes (metric cards, charts, stat widgets). Ensure they render to HTML or return data structures admin templates can consume.

    3. **ADMIN-03 (Admin inlines):** Verify `admin/generator.py` can generate inline admin classes from django-matt model definitions. Check it handles TabularInline and StackedInline.

    4. **GQL-01 (Strawberry schema):** Verify `graphql/schema.py` and `graphql/codegen.py` auto-generate Strawberry types from Django models. Check `HAS_STRAWBERRY` guard. Verify types include all model fields.

    5. **GQL-02 (DataLoaders):** Verify `graphql/dataloaders.py` provides generic DataLoader factory for Django models to prevent N+1 queries in GraphQL resolvers.

    6. **GQL-03 (GraphQL endpoint):** Verify `graphql/views.py` provides a view that serves GraphQL alongside REST. Check it handles both GET (GraphiQL UI) and POST (query execution).

    7. **HTMX-01 (Response helpers):** Verify `htmx/response.py` provides functions/classes to set HX-Trigger, HX-Swap, HX-Redirect, HX-Push-Url headers on responses.

    8. **HTMX-02 (Livewire-style):** Verify `htmx/components.py` provides reactive component base class with event handling.

    9. **COMP-01 (Component system):** Verify `components/serving.py` serves backend-rendered components.

    Fix any datetime.utcnow() or asyncio.get_event_loop() deprecations found. Ensure all optional imports use try/except with HAS_* flags.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_admin_module.py tests/test_graphql.py tests/test_htmx.py tests/test_components.py -x -q</automated>
  </verify>
  <done>All admin, GraphQL, HTMX, and component modules pass existing tests. Any deprecations fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add requirement-aligned tests for admin, GraphQL, HTMX, components</name>
  <files>tests/test_admin_module.py, tests/test_graphql.py, tests/test_htmx.py, tests/test_components.py</files>
  <behavior>
    - Test: Admin ModelAdmin base registers with Django admin site
    - Test: Dashboard widget renders with title and value
    - Test: Admin generator produces inline class from model
    - Test: Strawberry type generated from Django model has correct fields
    - Test: DataLoader batches multiple ID lookups into single query
    - Test: GraphQL view handles POST with query and returns JSON result
    - Test: HTMX response helper sets HX-Trigger header correctly
    - Test: HTMX response helper sets HX-Redirect header correctly
    - Test: Livewire-style component handles event and re-renders
    - Test: Backend component renders to HTML string
  </behavior>
  <action>
    Add or extend test classes to cover each requirement:

    1. `tests/test_admin_module.py`:
       - Test Unfold ModelAdmin registration (with `pytest.importorskip("unfold")` guard)
       - Test dashboard widget instantiation and render output
       - Test generator produces TabularInline for a model FK

    2. `tests/test_graphql.py`:
       - Test schema codegen from a simple Django model (use `pytest.importorskip("strawberry")`)
       - Test DataLoader batches lookups (mock queryset, verify single filter call with list of IDs)
       - Test GraphQL view POST returns valid JSON with `data` key

    3. `tests/test_htmx.py`:
       - Test HtmxResponse or response helper sets `HX-Trigger` header
       - Test HX-Redirect, HX-Swap, HX-Push-Url header helpers
       - Test LiveComponent event handling cycle

    4. `tests/test_components.py`:
       - Test component render produces HTML string
       - Test component serving via view returns rendered HTML

    Use existing test patterns. Guard optional deps with `pytest.importorskip()`.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_admin_module.py tests/test_graphql.py tests/test_htmx.py tests/test_components.py -x -q</automated>
  </verify>
  <done>Admin, GraphQL, HTMX, and component requirements verified by tests. All pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_admin_module.py tests/test_graphql.py tests/test_htmx.py tests/test_components.py -x -q` all pass
</verification>

<success_criteria>
Admin Unfold integration works with fallback; GraphQL schema generates from models with DataLoader N+1 prevention; HTMX helpers set correct headers; components render to HTML. All verified by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-observability-and-completion/07-04-SUMMARY.md`
</output>
