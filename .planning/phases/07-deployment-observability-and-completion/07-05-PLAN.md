---
phase: 07-deployment-observability-and-completion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/ai/base.py
  - django_matt/ai/embeddings.py
  - django_matt/ai/rag.py
  - django_matt/ai/vectorstore.py
  - django_matt/ml/base.py
  - django_matt/pagination/base.py
  - django_matt/pagination/page_number.py
  - django_matt/pagination/limit_offset.py
  - django_matt/pagination/cursor.py
  - django_matt/filtering/backends.py
  - django_matt/filtering/filterset.py
  - django_matt/filtering/search.py
  - django_matt/throttling/backends.py
  - django_matt/throttling/throttles.py
  - django_matt/throttling/decorators.py
  - tests/test_ai_context.py
  - tests/test_ml.py
  - tests/test_pagination.py
  - tests/test_filtering.py
  - tests/test_throttling.py
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - ML-01
  - ML-02
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "LLM integration helpers manage prompts and parse responses"
    - "Embedding generation produces vectors from text"
    - "RAG pipeline retrieves relevant documents and augments prompts"
    - "IDE context generation exports project structure for AI tools"
    - "Vector storage with pgvector stores and queries embeddings"
    - "Structured output parsing extracts typed data from LLM responses"
    - "Page number pagination returns correct page with count"
    - "Limit/offset pagination returns correct slice"
    - "Cursor pagination returns next/previous cursors"
    - "Filtering backend filters queryset by field values"
    - "Search filters queryset by text search across fields"
    - "Throttling limits request rate per user/IP"
  artifacts:
    - path: "django_matt/ai/base.py"
      provides: "LLM integration with prompt management"
      contains: "class.*LLM|prompt"
    - path: "django_matt/ai/rag.py"
      provides: "RAG pipeline utilities"
      contains: "class.*RAG|retrieve"
    - path: "django_matt/pagination/cursor.py"
      provides: "Cursor-based pagination"
      contains: "class.*CursorPagination"
    - path: "django_matt/filtering/filterset.py"
      provides: "FilterSet for queryset filtering"
      contains: "class.*FilterSet"
    - path: "django_matt/throttling/throttles.py"
      provides: "Rate limiting throttles"
      contains: "class.*Throttle"
  key_links:
    - from: "django_matt/ai/rag.py"
      to: "django_matt/ai/vectorstore.py"
      via: "RAG retrieves from vector store"
      pattern: "vectorstore|vector_store|similarity"
    - from: "django_matt/ai/embeddings.py"
      to: "django_matt/ai/vectorstore.py"
      via: "embeddings stored in vector store"
      pattern: "store|save|insert"
    - from: "django_matt/filtering/backends.py"
      to: "django_matt/filtering/filterset.py"
      via: "backend applies filterset to queryset"
      pattern: "filterset|FilterSet"
---

<objective>
Verify and complete AI/ML modules (LLM helpers, embeddings, RAG, IDE context, vector storage, structured output) and performance modules (pagination, filtering, throttling). All modules have existing implementations -- the work is auditing for correctness, fixing deprecations, and ensuring test coverage.

Purpose: AI/ML integration and data access patterns reach v1 quality.
Output: Verified modules with requirement-aligned tests.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-deployment-observability-and-completion/07-RESEARCH.md
@django_matt/ai/base.py
@django_matt/ai/embeddings.py
@django_matt/ai/rag.py
@django_matt/ai/vectorstore.py
@django_matt/ml/base.py
@django_matt/pagination/base.py
@django_matt/pagination/page_number.py
@django_matt/pagination/limit_offset.py
@django_matt/pagination/cursor.py
@django_matt/filtering/backends.py
@django_matt/filtering/filterset.py
@django_matt/filtering/search.py
@django_matt/throttling/backends.py
@django_matt/throttling/throttles.py
@django_matt/throttling/decorators.py
@tests/test_ai_context.py
@tests/test_ml.py
@tests/test_pagination.py
@tests/test_filtering.py
@tests/test_throttling.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit AI/ML and performance modules for correctness</name>
  <files>
    django_matt/ai/base.py,
    django_matt/ai/embeddings.py,
    django_matt/ai/rag.py,
    django_matt/ai/vectorstore.py,
    django_matt/ml/base.py,
    django_matt/pagination/base.py,
    django_matt/pagination/cursor.py,
    django_matt/filtering/backends.py,
    django_matt/filtering/filterset.py,
    django_matt/throttling/backends.py,
    django_matt/throttling/throttles.py
  </files>
  <action>
    Read all module files and audit against requirements:

    1. **AI-01 (LLM helpers):** Verify `ai/base.py` has prompt management (template rendering, response parsing). Check LLM provider abstraction supports multiple providers (OpenAI, Anthropic, etc.) with `HAS_*` guards.

    2. **AI-02 (Embeddings):** Verify `ai/embeddings.py` can generate embedding vectors from text via configurable provider. Check for proper async support.

    3. **AI-03 (RAG pipeline):** Verify `ai/rag.py` implements retrieve-then-augment pattern: query vector store for similar documents, inject into prompt context, call LLM.

    4. **AI-04 (IDE context):** Verify `ai/ide/` and `ai/context/` export project structure. This was already tested in Phase 3 (DX-06) but verify the module-level API exists.

    5. **ML-01 (Vector storage):** Verify `ai/vectorstore.py` supports pgvector operations: insert, similarity search, delete. Check `HAS_PGVECTOR` guard.

    6. **ML-02 (Structured output):** Verify `ml/` module parses structured output from LLM responses (JSON mode, function calling results). Check llamacpp/localai/vllm backends have common interface.

    7. **PERF-01 (Pagination):** Verify page_number, limit_offset, cursor paginators all implement a common interface. Check cursor pagination handles ordering correctly.

    8. **PERF-02 (Filtering):** Verify FilterBackend applies FilterSet to queryset. Check search across multiple fields works. Check ordering support.

    9. **PERF-03 (Throttling):** Verify throttle classes (per-user, per-IP, anon) use configurable backends (cache, redis). Check decorator and middleware integration.

    Fix any `datetime.utcnow()` or `asyncio.get_event_loop()` found. Ensure all optional imports use try/except with `HAS_*` flags.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_ai_context.py tests/test_ml.py tests/test_pagination.py tests/test_filtering.py tests/test_throttling.py -x -q</automated>
  </verify>
  <done>All AI/ML and performance modules pass existing tests. Deprecations fixed. Optional import guards verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add requirement-aligned tests for AI/ML and performance modules</name>
  <files>tests/test_ai_context.py, tests/test_ml.py, tests/test_pagination.py, tests/test_filtering.py, tests/test_throttling.py</files>
  <behavior>
    - Test: LLM helper renders prompt template with variables
    - Test: Embedding generator returns vector list from text input (mocked provider)
    - Test: RAG pipeline retrieves docs from vector store and augments prompt (mocked)
    - Test: Vector store insert and similarity search return ranked results (mocked pgvector)
    - Test: Structured output parser extracts typed fields from JSON LLM response
    - Test: PageNumberPagination returns page with count, next_page, previous_page
    - Test: LimitOffsetPagination returns correct slice with offset metadata
    - Test: CursorPagination returns cursor tokens for next/previous navigation
    - Test: FilterBackend with FilterSet filters queryset by exact/contains/range
    - Test: Search backend filters across multiple text fields
    - Test: UserRateThrottle limits to N requests per period, returns 429 on exceeded
    - Test: AnonRateThrottle uses IP-based tracking
  </behavior>
  <action>
    Add or extend test classes:

    1. `tests/test_ai_context.py` -- Add `TestLLMHelpers`:
       - Test prompt template rendering with variable substitution
       - Test response parsing extracts content from provider response format
       - Mock the LLM provider (no real API calls)

    2. `tests/test_ml.py` -- Add/extend:
       - Test embedding generation returns list of floats (mock provider)
       - Test RAG pipeline flow: insert docs -> query -> get augmented prompt (mock vector store)
       - Test vector store similarity_search returns ordered results (mock pgvector)
       - Test structured output parser handles JSON response from LLM

    3. `tests/test_pagination.py` -- Add if missing:
       - Test PageNumberPagination with 25 items, page_size=10, page=2 returns items 11-20
       - Test LimitOffsetPagination with limit=5, offset=10 returns items 10-14
       - Test CursorPagination returns valid cursor tokens, next page uses cursor

    4. `tests/test_filtering.py` -- Add if missing:
       - Test FilterSet with exact field match filters queryset
       - Test search across title+description fields
       - Test ordering by field name ascending/descending

    5. `tests/test_throttling.py` -- Add if missing:
       - Test UserRateThrottle allows N requests, blocks N+1 with 429
       - Test AnonRateThrottle tracks by IP address
       - Test throttle decorator on view function

    Use `pytest.importorskip()` for optional deps. Mock all external APIs.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_ai_context.py tests/test_ml.py tests/test_pagination.py tests/test_filtering.py tests/test_throttling.py -x -q</automated>
  </verify>
  <done>AI/ML helpers, pagination modes, filtering, search, and throttling all verified by tests.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_ai_context.py tests/test_ml.py tests/test_pagination.py tests/test_filtering.py tests/test_throttling.py -x -q` all pass
- Full suite: `uv run pytest tests/ -x -q` still passes
</verification>

<success_criteria>
AI/ML modules handle prompts, embeddings, RAG, and structured output. Pagination supports page number, limit/offset, and cursor. Filtering supports field filters, search, and ordering. Throttling limits request rates. All verified by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-observability-and-completion/07-05-SUMMARY.md`
</output>
