---
phase: 07-deployment-observability-and-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/deploy/environments.py
  - django_matt/config/components/database.py
  - django_matt/config/environments/production.py
  - django_matt/config/environments/staging.py
  - django_matt/config/settings/prod.py
  - django_matt/config/settings/staging.py
  - django_matt/config/settings/dev.py
  - tests/test_deploy.py
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06

must_haves:
  truths:
    - "Every deployment provider template emits CONN_MAX_AGE=0 for ASGI"
    - "Docker template defaults to ASGI server (uvicorn/granian)"
    - "All config environment files use CONN_MAX_AGE=0 not 600 or None"
    - "Tests verify CONN_MAX_AGE=0 enforcement across all providers"
  artifacts:
    - path: "django_matt/deploy/environments.py"
      provides: "Environment config with CONN_MAX_AGE=0 enforced"
      contains: "conn_max_age.*0"
    - path: "tests/test_deploy.py"
      provides: "CONN_MAX_AGE enforcement tests for all providers"
      contains: "CONN_MAX_AGE"
  key_links:
    - from: "django_matt/deploy/environments.py"
      to: "django_matt/deploy/providers/*.py"
      via: "environment config propagation"
      pattern: "conn_max_age"
    - from: "django_matt/config/environments/production.py"
      to: "DATABASES"
      via: "settings dict"
      pattern: "CONN_MAX_AGE.*0"
---

<objective>
Enforce CONN_MAX_AGE=0 across all ASGI deployment templates and config environments. This is a known production blocker from STATE.md -- Django ticket #33497 means persistent connections leak under ASGI.

Purpose: Every developer deploying a django-matt app gets safe database connection settings out of the box.
Output: Fixed deployment configs, fixed config templates, CONN_MAX_AGE enforcement tests.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment-observability-and-completion/07-RESEARCH.md
@django_matt/deploy/environments.py
@django_matt/deploy/docker.py
@django_matt/deploy/base.py
@django_matt/config/components/database.py
@django_matt/config/environments/production.py
@django_matt/config/environments/staging.py
@django_matt/config/settings/prod.py
@django_matt/config/settings/staging.py
@django_matt/config/settings/dev.py
@tests/test_deploy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CONN_MAX_AGE=0 enforcement in all deploy and config files</name>
  <files>
    django_matt/deploy/environments.py,
    django_matt/config/components/database.py,
    django_matt/config/environments/production.py,
    django_matt/config/environments/staging.py,
    django_matt/config/settings/prod.py,
    django_matt/config/settings/staging.py,
    django_matt/config/settings/dev.py
  </files>
  <action>
    Systematically audit and fix every file that sets CONN_MAX_AGE to a non-zero value:

    1. `deploy/environments.py` line 253: Change `"conn_max_age": 600` to `"conn_max_age": 0` (production environment override)
    2. `config/components/database.py` line 65: Change fallback from `None if env == "production" else 600` to `0` (always 0 for ASGI safety)
    3. `config/environments/production.py` line 84: Change default from `600` to `0` in `os.environ.get("DB_CONN_MAX_AGE", 600)` -> `os.environ.get("DB_CONN_MAX_AGE", "0")`
    4. `config/environments/staging.py` line 66: Same fix, default `600` -> `0`
    5. `config/settings/prod.py` line 87: Change `"CONN_MAX_AGE": None` to `"CONN_MAX_AGE": 0`
    6. `config/settings/staging.py` line 67: Change `"CONN_MAX_AGE": 300` to `"CONN_MAX_AGE": 0`
    7. `config/settings/dev.py` line 73: Change `"CONN_MAX_AGE": 60` to `"CONN_MAX_AGE": 0`

    Add a comment on each CONN_MAX_AGE=0 line: `# ASGI requires 0 (Django #33497)`

    Also verify `deploy/docker.py` DockerfileGenerator defaults to ASGI server (uvicorn or granian). If it defaults to gunicorn+wsgi, update CMD to use `gunicorn --worker-class uvicorn.workers.UvicornWorker` pattern.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && grep -rn "CONN_MAX_AGE\|conn_max_age" django_matt/deploy/ django_matt/config/ | grep -v "__pycache__" | grep -v "=.*0"</automated>
  </verify>
  <done>Zero non-zero CONN_MAX_AGE values remain in any deploy or config file. Every instance is 0 with ASGI comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CONN_MAX_AGE enforcement tests for all providers</name>
  <files>tests/test_deploy.py</files>
  <behavior>
    - Test: Each deployment provider (Fly, Railway, Render, AWS, Docker) generates config with CONN_MAX_AGE=0
    - Test: Environment config for production/staging/dev all have conn_max_age=0
    - Test: Docker template CMD uses ASGI server (uvicorn/granian), not WSGI
    - Test: config/components/database.py get_database_config() returns CONN_MAX_AGE=0 by default
  </behavior>
  <action>
    Add a `TestConnMaxAgeEnforcement` test class to `tests/test_deploy.py` with tests that:

    1. Import each provider (FlyioProvider, RailwayProvider, RenderProvider, AwsProvider) and generate their config output, assert CONN_MAX_AGE=0 appears or conn_max_age=0 is set
    2. Import `EnvironmentConfig` from `deploy/environments.py`, create production/staging environments, assert `conn_max_age == 0`
    3. Import `DockerfileGenerator` from `deploy/docker.py`, generate a Dockerfile, assert it uses uvicorn or granian (not `python manage.py runserver`)
    4. Test `get_database_config()` from `config/components/database.py` with no env vars set, assert returned dict has `CONN_MAX_AGE` equal to `0`

    Use `pytest.importorskip()` for any optional dependencies. Follow established test patterns in the existing test file.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_deploy.py -x -q -k "conn"</automated>
  </verify>
  <done>All CONN_MAX_AGE enforcement tests pass. Every provider and config path verified to emit CONN_MAX_AGE=0.</done>
</task>

</tasks>

<verification>
- `grep -rn "CONN_MAX_AGE\|conn_max_age" django_matt/deploy/ django_matt/config/ | grep -v "__pycache__" | grep -v "=.*0"` returns empty (no non-zero values)
- `uv run pytest tests/test_deploy.py -x -q` all pass
</verification>

<success_criteria>
Every CLI-generated deployment config (Docker, Fly.io, Railway, Render, AWS) sets CONN_MAX_AGE=0 -- verified by tests and grep audit.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-observability-and-completion/07-01-SUMMARY.md`
</output>
