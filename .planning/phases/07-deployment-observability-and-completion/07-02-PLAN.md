---
phase: 07-deployment-observability-and-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - django_matt/observability/logging.py
  - django_matt/observability/metrics.py
  - django_matt/observability/tracing.py
  - django_matt/observability/views.py
  - django_matt/inspector/middleware.py
  - tests/test_observability.py
  - tests/test_inspector.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04

must_haves:
  truths:
    - "Structured logging produces valid JSON with configurable formatters"
    - "Prometheus metrics endpoint responds with metric data"
    - "OTEL tracing emits spans for request handling"
    - "Request inspector captures request/response pairs in dev mode"
  artifacts:
    - path: "django_matt/observability/logging.py"
      provides: "Structured logging with JSONFormatter, PrettyJSONFormatter, ColoredTextFormatter"
      contains: "class JSONFormatter"
    - path: "django_matt/observability/metrics.py"
      provides: "Prometheus-compatible metrics collection"
      contains: "MetricsManager"
    - path: "django_matt/observability/tracing.py"
      provides: "OpenTelemetry tracing integration"
      contains: "TracingManager"
    - path: "tests/test_observability.py"
      provides: "Observability tests covering logging, metrics, tracing"
      min_lines: 2500
  key_links:
    - from: "django_matt/observability/middleware.py"
      to: "django_matt/observability/metrics.py"
      via: "middleware records request metrics"
      pattern: "metrics"
    - from: "django_matt/observability/middleware.py"
      to: "django_matt/observability/tracing.py"
      via: "middleware creates tracing spans"
      pattern: "tracing|span"
---

<objective>
Verify and complete the observability module (structured logging, Prometheus metrics, OTEL tracing) and request inspector. All modules already exist with substantial code -- the work is auditing for gaps, fixing any issues, and ensuring success-criteria-aligned test coverage.

Purpose: A developer using django-matt gets production-grade observability out of the box.
Output: Verified observability module with complete test coverage, working inspector.
</objective>

<execution_context>
@/Users/mattjaikaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mattjaikaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-deployment-observability-and-completion/07-RESEARCH.md
@django_matt/observability/logging.py
@django_matt/observability/metrics.py
@django_matt/observability/tracing.py
@django_matt/observability/middleware.py
@django_matt/observability/views.py
@django_matt/inspector/middleware.py
@tests/test_observability.py
@tests/test_inspector.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix observability module gaps</name>
  <files>
    django_matt/observability/logging.py,
    django_matt/observability/metrics.py,
    django_matt/observability/tracing.py,
    django_matt/observability/views.py,
    django_matt/inspector/middleware.py
  </files>
  <action>
    Read all observability module files and the inspector module. Audit against requirements:

    1. **OBS-01 (Structured logging):** Verify JSONFormatter produces valid JSON output with timestamp, level, message, and optional extras. Verify PrettyJSONFormatter and ColoredTextFormatter work. Fix any issues found.

    2. **OBS-02 (Prometheus metrics):** Verify MetricsManager exposes a `/metrics` endpoint that returns Prometheus-format text. Ensure `prometheus_client` is imported with proper `HAS_PROMETHEUS` guard. Verify views.py has a metrics endpoint. If the metrics view isn't wired to return `prometheus_client.generate_latest()`, fix it.

    3. **OBS-03 (OTEL tracing):** Verify TracingManager configures OTEL spans for request handling. Check that OTLP exporter is preferred over deprecated Jaeger exporter. Ensure `opentelemetry-sdk` is imported with proper `HAS_OTEL` guard.

    4. **OBS-04 (Request inspector):** Verify inspector middleware captures request/response pairs. Check it has proper dev-only gating (not active in production).

    Fix any datetime.utcnow() usage found (use datetime.now(UTC) per established pattern).
    Fix any asyncio.get_event_loop() usage (use asyncio.get_running_loop() or asyncio.to_thread()).
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_observability.py tests/test_inspector.py -x -q</automated>
  </verify>
  <done>All observability modules pass existing tests. Any datetime.utcnow() or asyncio.get_event_loop() deprecations fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add success-criteria-aligned observability tests</name>
  <files>tests/test_observability.py, tests/test_inspector.py</files>
  <behavior>
    - Test: JSONFormatter.format() produces valid JSON string with required fields (timestamp, level, message)
    - Test: MetricsManager with prometheus_client records request count/latency metrics
    - Test: Prometheus metrics view returns text/plain content with metric lines
    - Test: TracingManager creates spans with correct operation names
    - Test: Inspector middleware captures request method, path, status code, response body
    - Test: Inspector is disabled when DEBUG=False
  </behavior>
  <action>
    Add tests to verify success criteria #2 from the roadmap. Specifically:

    1. In `tests/test_observability.py`, add `TestStructuredLogging`:
       - Create a JSONFormatter, format a LogRecord, parse output with `orjson.loads()`, assert keys present
       - Verify log level, message, timestamp fields

    2. Add `TestPrometheusMetrics`:
       - Create MetricsManager, record a request metric, verify counter incremented
       - Mock a metrics view request, verify response contains prometheus text format

    3. Add `TestOTELTracing`:
       - Create TracingManager with mock OTEL SDK, start a span, verify span attributes
       - Use `pytest.importorskip("opentelemetry")` guard

    4. In `tests/test_inspector.py`, add tests verifying:
       - Middleware captures request/response data
       - Inspector storage retrieves captured requests

    Follow existing test patterns in the file. Use `pytest.importorskip()` for optional deps.
  </action>
  <verify>
    <automated>cd /Users/mattjaikaran/dev/django-matt && uv run pytest tests/test_observability.py tests/test_inspector.py -x -q</automated>
  </verify>
  <done>Observability tests verify structured logging JSON output, Prometheus metrics endpoint, OTEL span creation, and inspector capture.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_observability.py tests/test_inspector.py -x -q` all pass
- No `datetime.utcnow()` or `asyncio.get_event_loop()` in observability code
</verification>

<success_criteria>
Structured logs produce valid JSON; Prometheus metrics endpoint responds; OTEL tracing emits spans; inspector captures dev requests. All verified by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-deployment-observability-and-completion/07-02-SUMMARY.md`
</output>
