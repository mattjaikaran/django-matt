[project]
name = "multitenant-saas"
version = "1.0.0"
description = "Multi-tenant SaaS example with events and feature flags"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"

dependencies = [
    "django>=5.2",
    "django-matt>=0.1.0",
    "pydantic>=2.0",
    "orjson>=3.9",
    "uvicorn[standard]>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "mt_project.settings"
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"
